info:
  name: Diagnostics Agent
  type: http
  seq: 2

http:
  method: get
  url: https://localhost:8099/api/agent/diagnostics
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "question": "Oii"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
